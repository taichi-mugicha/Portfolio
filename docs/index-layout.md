# indexページ レイアウト仕様

ポートフォリオのトップ（作品一覧）ページのレイアウト仕様と要望をまとめる。
実装は [index.html](../index.html) 内の `.top` セクション。作品カードをタップすると[作品ページ](work-detail-layout.md)（ボトムシート）が開く。

---

## 全体方針

- モバイルファースト（コンテンツ幅 `max-width: 430px` の1カラム、中央寄せ）。
- ページ背景は `#dcdcdc`、コンテンツ面は白 `--bg: #ffffff`。スマホ実機に載せたような見え方。
- 初回ロード時に挨拶スプラッシュ（「こんにちは 👋」）を表示。

## フォント

和文は **Noto Sans JP**（Google Fonts / `<head>` で読み込み）。ウェイトは 400・500・600・700 を読み込む。
欧文はこれまでどおり端末のシステムフォント（`-apple-system` ほか）。読み込めない環境では端末のゴシックにフォールバックする。

### タイプスケール（index・作品詳細 共通）

| 役割 | サイズ | 字間 | 行間 | 太さ |
|---|---|---|---|---|
| ヒーロー見出し | 28 / 32 / 36px | -0.02em | 1.3 | 700 |
| 作品タイトル・結果の数値 | 32px | -0.02em | 1.1 | 700 |
| セクション見出し | 22 / 20px | -0.02em | 1.3 | 600 |
| カードタイトル・ロゴ | 16px | +0.05em | 1.5 | 600（ロゴ700） |
| 本文・概要・関連作品名 | 14px | +0.05em | 1.65 | 400 |
| タグ・補助テキスト | 12px | 0 | 1.5 | 400 |
| ラベル・キャプション・番号 | 11px | 0 | 1.5 | 400（ラベル600） |

- 字間は**20px以上の見出しが -0.02em**、**16px・14px が +0.05em**（和文を少し開けて読みやすく）、12px以下は 0。
- 行間は**見出し 1.1〜1.3 / 本文 1.65 / 補助 1.5** の3系統。
- 太さは **400 / 600 / 700** の3段のみ。

## カラートークン

| 変数 | 値 | 用途 |
|---|---|---|
| `--ink` | `#0a0a0a` | 基本の文字色 |
| `--muted` | `#737373` | 補助テキスト（タグ・番号・eyebrow） |
| `--bg` | `#ffffff` | コンテンツ面の背景 |
| `--ph` | `#ececec` | 画像プレースホルダー |
| ページ地色 | `#dcdcdc` | body 背景 |

## 構成（上から順に）

1. **ナビバー** — 高さ52px。左にロゴ `TAICHI ITO`（`■` プレフィックス）、右にハンバーガーメニュー（現状は見た目のみ）。
2. **ヒーロー見出し** — 28px / bold / line-height 1.3 / 字間 -0.02em。現在のコピーは `Make it essential. / Make it clear.`（2行）。
3. **作品グリッド** — 1カラム / gap 28px。

eyebrow（`UI / UX デザイナー — IoT・アプリ`）とSelected行（`Selected Work — 2020 / 2023` ＋ 総件数）は2026.09に削除した。

## レスポンシブ

モバイル（〜639px）が正で、それ以外は上書きのみ。ブレークポイントは2つ。

| 幅 | コンテンツ幅 | グリッド | ヒーロー見出し |
|---|---|---|---|
| 〜639px | 430px | 1カラム / gap 28px | 28px |
| 640px〜 | 760px | 2カラム / gap 36px・24px | 32px |
| 1024px〜 | 1100px | 3カラム / gap 44px・28px | 36px |

- 左右パディングも 20px → 32px → 48px と広げる（safe-area-inset 込み）。
- `@media (hover:hover) and (pointer:fine)` の環境だけ、カードのホバーで
  サムネを `opacity:.86` に落とす。タップ時の `scale(.985)` はそのまま。
- 拡大縮小は禁止しない（viewport meta から `maximum-scale` / `user-scalable=no` を外した）。

## 作品カード

- サムネイル：アスペクト比 **4:3** / 角丸6px / `works/images/{slug}/01.png` を `object-fit: cover`。
- タップ時に `scale(.985)` で軽く沈む。
- メタ：タイトル（16px / 600）＋ カテゴリタグ1つ（muted）を左、通し番号 `0X` を右（総件数は併記しない）。
- タップで該当作品のボトムシートを開く。

## スプラッシュ（挨拶）

- 「こんにちは 👋」を中央に表示。手を振るアニメーション付き。
- 初回訪問のみ最低 **1.2秒** 表示（一瞬すぎて見えない問題への対応）。再訪時は `sessionStorage` を見て即座に消す。
- `prefers-reduced-motion` でアニメーション停止。
- 調整値は `:root` 変数に集約（`--wave-duration` など）。

---

## 要望・意図（随時追記）

- スマホでの閲覧を主体に考えたい。PCでも破綻しない範囲でよい。
- 2026.09 レスポンシブ対応。PCで430pxの1カラムのまま余白が空いていたので、
  タブレット2カラム / デスクトップ3カラムに広げた。トーン（余白多め・モノトーン）は据え置き。
- 一覧はごちゃつかせず、作品を淡々と並べる。装飾より作品そのものを見せる。
- 作品数は現状7件。増減に追従できるようデータ駆動で描画している。
- 2026.07 トップのデザイン方向性を再検討中。「日本語が美しく見える」ことを軸に3案（A: 縦書き明朝 / B: 目次型明朝 / C: ゴシック大級数グリッド）を [index-design-drafts.html](../index-design-drafts.html) で比較検討している（未決定）。

- 2026.09 トップの情報量を削る。eyebrow・Selected行（年号と総件数）を削除し、カードの通し番号も `0X` のみに。
  ナビ直下からヒーロー見出しが始まる形にした。
- 2026.09 一覧カードのタグは成果物の種類を示すカテゴリ1つだけにする（UIデザイン / アイコンデザイン など）。
- 2026.09 和文フォントを Google Fonts の Noto Sans JP で明示的に読み込む（端末依存をやめる）。
- 2026.09 文字組みを整理。サイズを7段（36-32-22/20-16-14-12-11）、行間3系統、太さ3段、字間2値に丸めて統一した。
- 2026.09 16px・14pxの字間を +0.05em に。和文の本文が詰まって見えたため。
- 2026.09 ヒーローコピーは Garamond を試したのち Noto Sans JP に戻し、サイズを28px（PCは36px）まで落とした。見出しで語らず作品を見せる方向。

> このセクションはやり取りの中で出た要望を貯めていく欄。決定した仕様は上の「構成」側へ反映する。
